# Source Images

## Used By

Pages:

- none at runtime

Components:

- none at runtime

## Purpose

This directory stores raw source materials only.

Examples:

- original large images
- uncropped outputs
- sticker sheets
- references
- source textures

## Naming

Use lowercase `kebab-case` when possible, but preserving external source names is acceptable if the asset has not been normalized yet.

## Current Status

- Existing directories: yes
- Existing assets: none committed in this phase
- Missing assets: not applicable

## Runtime Rule

Do not reference files inside `_source/` from application code.
